# Dependencies
node_modules/

# Playwright output
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/

# Auth storage state (contains session cookies — never commit)
/playwright/.auth/
.auth/

# Personal env overrides (SMTP credentials etc.) — loaded by envLoader as the
# base file, never committed. Committed env.local/dev/qa hold no real secrets.
.env

# Reporting output
allure-results/
allure-report/
logs/

# Claude Code — the shared .claude/ folder IS committed; only personal
# local overrides are ignored.
.claude/settings.local.json

#Testing context
Testing video/

# Video annotator — generated keyframes/JSON, the Python venv, and the optional
# generated cursor templates. All reproducible: see
# tools/video-annotator/README.md.
.video-annotations/
tools/video-annotator/.venv/
tools/video-annotator/cursors/

# NOTE: package-lock.json is intentionally tracked — CI uses `npm ci`,
# which requires it.


# Local SQL Server backup (12.9GB real client data — never commit)
db backup/
*.bak
docker-e2e/.env
