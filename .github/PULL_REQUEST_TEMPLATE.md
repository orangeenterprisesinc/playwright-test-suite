## What

<!-- One line: which feature/ticket this spec covers. -->

- Jira ticket: WEBPET-
- Spec file(s):

## Checklist

- [ ] Ran the affected spec(s) with `npm run test:dev` against dev staging — green (paste the summary line below)
- [ ] `npm run typecheck` green
- [ ] `npm run lint` green
- [ ] Runner check green (`npm run runner:check` or `npm run webpet:runner:check`) and data added via the `.csv`, not the `.json`
- [ ] `testCaseId` / requirement annotations present and traceable to the ticket
- [ ] Assertions reflect the ticket's expected behavior (not just "page rendered")
- [ ] Parallel-safe at 2 workers (run-unique names, no cross-spec ordering); serial-only flagged if the workflow holds a single session
- [ ] No framework files touched (fixtures, config, utils, scripts, CI, `.claude/`)

## Local run result

```text
<paste the test:dev summary here>
```
