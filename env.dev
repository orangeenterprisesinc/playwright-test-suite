# DEV environment — placeholder values, fill in when a DEV deployment exists.
# Run with:  npm run test:dev   (or: cross-env TEST_ENV=dev npx playwright test)

BASE_URL=https://dev.pet-tiger.example.com
API_URL=https://dev.pet-tiger.example.com/api
USER_NAME=CHANGE_ME
PASSWORD=CHANGE_ME
TEST_DATA_SOURCE=json
